# macs is a list of strings of any mac addresses to monitor (phone's wifi mac address).
macs = ["00:00:00:00:00:00"]
# notify_timeout is how many seconds before sending a push notification to verify if the phone is gone.
# The default value of 900 is a fairly reasonable time limit for an iPhone
notify_timeout = 900
# prowl is used to wake the phone up on the network, an API key is required.
prowl_api_key = "0f0f0f0f0f0f0f0f0f0f0f0f0f0f0f0f0"
